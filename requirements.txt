# IQBot v2 Requirements
# Core
websockets>=13.0
python-dotenv>=1.0
cryptography>=43.0
aiohttp>=3.10

# Telegram Bot
python-telegram-bot>=21.0

# Redis (redis>=4.2 includes redis.asyncio — aioredis standalone package not needed)
redis>=5.0

# Database
# (sqlite3 is in the stdlib; aiosqlite removed — only sync access used)

# PM2 management
# (system-level, not pip)

# Affiliate listener
telethon>=1.37

# Utils
